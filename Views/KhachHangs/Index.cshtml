@model IEnumerable<thaison1210.Models.KhachHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDKhachHang }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDKhachHang }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDKhachHang })
        </td>
    </tr>
}

</table>
